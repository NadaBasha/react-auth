version: 0.2

phases:
  install:
    commands:
      - cd $CODEBUILD_SRC_DIR; npm install
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR; npm run build
  postbuild:
    commands:
      - cd $CODEBUILD_SRC_DIR/build

artifacts:
  files:
    - 'build/**/*'
  discard-paths: yes
